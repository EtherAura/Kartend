; Current INI: schemaVersion matches the build's kSettingsSchemaVersion. The
; expected steady-state shape — every modern build writes this on save. Load
; should be silent and lossless. Bump the value here whenever the schema
; version bumps, or this "no migration fires" fixture starts firing one.
[General]
schemaVersion=2
rememberSelection=true
pixmapCacheSizeMB=64
titleTintSaturation=200
