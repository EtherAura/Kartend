; Legacy INI: no schemaVersion key. Represents a settings file written by
; the build that predates Kartend-hdo2. SettingsManager::loadGeneralSettings
; should decode every key into GeneralSettings using either the stored value
; or the in-code default (no warnings, no mutation).
[General]
rememberSelection=false
pixmapCacheSizeMB=128
titleTintSaturation=120
