; Future INI: schemaVersion=99 — written by a newer build that knows
; keys this build doesn't. Load should emit a single qCWarning and then
; decode every key this build DOES know, leaving unknown keys silently
; ignored on the QSettings side.
[General]
schemaVersion=99
rememberSelection=false
pixmapCacheSizeMB=200
titleTintSaturation=50
