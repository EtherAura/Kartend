<?xml version="1.0" encoding="UTF-8"?>
<component type="desktop-application">
  <id>io.github.EtherAura.Kartend</id>
  <metadata_license>CC0-1.0</metadata_license>
  <project_license>GPL-3.0-only</project_license>
  <name>Kartend</name>
  <summary>Organize and launch multimedia collections</summary>
  <developer id="io.github.etheraura">
    <name>EtherAura</name>
  </developer>
  <description>
    <p>
      Kartend is a KDE-friendly Qt frontend for organizing, browsing, and launching
      multimedia collections with custom launchers.
    </p>
    <p>
      It supports large libraries through virtual scrolling, asynchronous artwork
      loading, collection hierarchies, persistent sessions, keyboard navigation,
      metadata sidebars, and configurable launch commands for media, emulators,
      RetroArch cores, or native applications.
    </p>
  </description>
  <url type="homepage">https://github.com/EtherAura/Kartend</url>
  <url type="bugtracker">https://github.com/EtherAura/Kartend/issues</url>
  <url type="vcs-browser">https://github.com/EtherAura/Kartend</url>
  <categories>
    <category>Utility</category>
  </categories>
  <keywords>
    <keyword>launcher</keyword>
    <keyword>collection</keyword>
    <keyword>media</keyword>
    <keyword>games</keyword>
  </keywords>
  <launchable type="desktop-id">io.github.EtherAura.Kartend.desktop</launchable>
  <provides>
    <binary>kartend</binary>
  </provides>
  <releases>
    <release version="0.0.15" date="2026-07-04">
      <description>
        <p>DAT audit fix:</p>
        <ul>
          <li>When one file's content is listed by more than one catalogue entry (e.g. a non-merged clone set that re-lists a shared entry), the audit no longer reports the duplicate entry as Missing while the file is present.</li>
        </ul>
      </description>
    </release>
    <release version="0.0.14" date="2026-06-20">
      <description>
        <p>Adds the DAT Manager — audit your collections against DAT files:</p>
        <ul>
          <li>See which items are present, wrong, or missing, reading inside .zip and .7z archives</li>
          <li>A dedicated audit browser: results tree, dual detail panes, status filters, a folder-as-item view, category grouping, and saved view presets — with layout, filters, and expanded rows remembered across sessions</li>
          <li>Clone-aware auditing with selectable Split, Merged, and Non-merged modes</li>
          <li>One-click Fix repacks and renames archives to match the DAT; mismatched files are quarantined</li>
          <li>DAT files can be downloaded, watched for updates, and re-downloaded in place</li>
        </ul>
        <p>Also: the mouse wheel now scrolls the focused dialog, and runtime system-accent changes update every colour live.</p>
      </description>
    </release>
    <release version="0.0.13" date="2026-06-11">
      <description>
        <p>Stability and performance release from a full-codebase audit:</p>
        <ul>
          <li>Smoother scrolling and cover flow on very large collections; cover artwork now reliably fills in</li>
          <li>Scan and database operations retry cleanly under concurrent load; cancelling a scan no longer blocks rescanning</li>
          <li>Search fixes: literal underscore/percent matching, no more snap-back when clearing a search quickly</li>
          <li>Gamepad input recovers automatically after a controller disconnects or sleeps</li>
          <li>Archive-backed items extract off-thread with a progress overlay and a decompression size cap</li>
          <li>A persistent warning banner appears if credentials ever fall back to unencrypted storage</li>
          <li>Play statistics are no longer lost when quitting right after launching an item</li>
        </ul>
      </description>
    </release>
    <release version="0.0.12" date="2026-05-30"/>
    <release version="0.0.11" date="2026-05-26"/>
    <release version="0.0.10" date="2026-05-25"/>
    <release version="0.0.9" date="2026-05-24"/>
    <release version="0.0.8" date="2026-05-20">
      <description>
        <p>
          Scrape budget gets a live readout: the scrape window now shows
          your ScreenScraper "requests today" count alongside the time
          the quota resets, and once the daily request or failed-lookup
          quota is reported exhausted the run stops on its own instead
          of burning the rest of the queue.
        </p>
        <p>
          Skip and Fill missing rescrape modes pre-filter the queue
          before any provider request fires: Skip drops items whose
          metadata is already present (database row or sidecar JSON on
          disk, so kart imports and older builds without a DB row are
          caught too); Fill missing drops items whose ticked checkboxes
          (metadata plus each selected media type) are all already on
          disk. A new "Refresh items scraped before" spinbox in scraper
          settings lets you mark items as eligible for refresh after a
          chosen number of days (default 30; 0 always skips covered
          items).
        </p>
        <p>
          Settings dialog redesigned with a sidebar layout: categories
          live in a navigation list down the left side next to the
          collection tree and a new search box, replacing the previous
          nested rows of tabs. Draggable dividers resize the rail and
          split its height between the tree and the category list, and
          the dialog remembers its size and divider positions between
          runs.
        </p>
        <p>
          Reliability and security: PDF manuals no longer crash large
          scrapes (non-image files are kept out of the image decoder);
          the scrape window's collection label updates as each
          collection is reached even when its items all fail; the
          scrape error list is scrollable and keeps far more detail;
          scrape logging masks ScreenScraper credentials in any URL
          written to scrape.log.
        </p>
      </description>
    </release>
    <release version="0.0.7" date="2026-05-17"/>
    <release version="0.0.6" date="2026-05-11">
      <description>
        <p>
          Pure decision logic for five managers (detail page, selection
          restore, attract, overlay, gamepad) extracted into helper
          namespaces with 102 new unit tests covering edge cases, hysteresis,
          boundary fallbacks, and priority ladders.
        </p>
        <p>
          Packaging and build polish: all CMake options namespaced to
          KARTEND_*, portable-release flag set for distros, gamepad/SDL2/zstd
          auto-detect toggles, an uninstall target, CPack tarballs, a
          tag-triggered release workflow with sha256 publishing, hardening
          flags split from performance flags, and Qt 6.4 as the new minimum.
        </p>
        <p>
          Reliability fixes: PlaylistManager export now uses QSaveFile +
          parent-dir fsync, failed SQL in QueryManager rescan and
          DbMigrations user-version writes now log instead of failing
          silently, PGO instrumentation actually reaches the compiler, and
          several Qt 6.4 portability fixes restore builds on Ubuntu 24.04.
        </p>
      </description>
    </release>
    <release version="0.0.5" date="2026-05-06">
      <description>
        <p>
          Substantial feature release: per-collection multi-launchers with chooser
          dialog, splash screens with placeholder artwork, expand mode with
          two-stage activation, regex title-exclusion, sortable date/file size,
          and a configurable items-page toolbar.
        </p>
        <p>
          Adds the Coverflow 3D view, horizontal scrolling layout, video previews
          and a Now Playing overlay, per-item metadata with custom fields, an
          artwork gallery, chronological launch history, per-item usage stats,
          attract mode, alias-parent links, playlists with M3U/JSON import/export,
          and a runtime text zoom HUD.
        </p>
        <p>
          Packaging adds Arch PKGBUILD and a Flatpak manifest. Tests gain an
          integration harness for UI-coordinator managers.
        </p>
      </description>
    </release>
    <release version="0.0.4" date="2026-04-25">
      <description>
        <p>
          Adds the attract-mode settings UI (enable toggle, idle timeout, scroll
          speed) and reworks the timeout reset to react to selection changes.
        </p>
      </description>
    </release>
    <release version="0.0.3" date="2026-04-24">
      <description>
        <p>
          Settings dialog polish, expanded build-script docs, and a fix for the
          shutdown crash where widget-pool callbacks could outlive their targets.
          Adds DatabaseManager lifecycle and path-resolution unit tests.
        </p>
      </description>
    </release>
    <release version="0.0.2" date="2026-04-21">
      <description>
        <p>
          Large refactor splitting the largest managers (Scroll, Query, Settings,
          Navigation, Interaction, Viewport, ItemWidget, MainWindow,
          SettingsDialog) into focused sibling translation units. Fixes a UBSan
          shutdown vptr violation and a missing include that broke the build on
          Ubuntu 24.04 libstdc++.
        </p>
      </description>
    </release>
    <release version="0.0.1" date="2026-04-18">
      <description>
        <p>
          Initial release. Virtual scrolling with widget pooling, async artwork
          loading with disk cache, collection hierarchy with subcollection
          navigation, session persistence, configurable launchers, full keyboard
          navigation, glide scroll animations, grid and list view modes, metadata
          sidebar, FTS5 search, gamepad support, and Gentoo ebuild packaging.
        </p>
      </description>
    </release>
  </releases>
  <content_rating type="oars-1.1" />
</component>
